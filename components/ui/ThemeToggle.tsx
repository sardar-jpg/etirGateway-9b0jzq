// ThemeToggle is disabled — app runs in dark mode only.
// This stub exists to satisfy any remaining imports without causing errors.
import React from 'react';
import { View } from 'react-native';

interface Props { size?: 'sm' | 'md' | 'lg' }

export function ThemeToggle(_props: Props) {
  return <View />;
}
